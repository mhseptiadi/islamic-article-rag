docker run -p 6333:6333 -p 6334:6334 -v $(pwd)/qdrant_storage:/qdrant/storage:z qdrant/qdrant

ollama run qwen2.5:7b
ollama run bge-m3

quran-rag-backend/
├── cmd/
│   ├── api/
│   │   └── main.go                 # Starts the live QnA HTTP/gRPC server
│   └── ingest/
│       └── main.go                 # CLI tool to run the paragraph-splitting pipeline
├── data/                        
│   ├── raw_articles/             # Drop your new .md files here
│   └── processed_articles/       # Move them here after successful ingestion
├── internal/
│   ├── config/
│   │   └── config.go               # Handles environment variables (Qdrant URL, API keys)
│   ├── handler/
│   │   └── qna_handler.go          # HTTP handler for POST /api/v1/ask
│   ├── model/
│   │   └── article.go              # Structs for Chunks, Payload, and Metadata
│   ├── repository/                 # DATA ACCESS LAYER
│   │   └── qdrant/
│   │       └── vector_repo.go      # Insert chunks & execute similarity search
│   └── service/                    # CORE BUSINESS LOGIC
│       ├── ingestion_service.go    # Loop for paragraph windowing, regex tagging, & embedding
│       ├── client_embedding.go     # Wrapper for external Embedding API (e.g., Cohere)
│       ├── client_llm.go           # Wrapper for external LLM API (e.g., Gemini/OpenAI)
│       └── qna_orchestrator.go     # Coordinates Qdrant -> LLM during QnA
├── pkg/
│   └── regexutil/
│       └── parser.go               # Custom regex patterns to extract "(QS. Al-Baqarah: 153)"
├── go.mod
└── go.sum

